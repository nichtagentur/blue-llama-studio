export function injectBlueLlama(userPrompt: string): string {
  return `${userPrompt.trim()}. The scene prominently features a cute, friendly blue llama character with bright blue fur as a key element in the video.`;
}
